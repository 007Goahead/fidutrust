# Client-side (exposed in the browser bundle - Vite requires the VITE_ prefix).
# Safe to expose: the publishable key only allows what Postgres RLS policies permit.
VITE_SUPABASE_URL=https://ypfzkgogobfmagiivpwz.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx

# Server-side only (used by /api/create-lead.ts). NEVER prefix with VITE_,
# never commit real values, never log them.
SUPABASE_SERVICE_ROLE_KEY=
CLICKUP_API_TOKEN=
CLICKUP_LIST_ID_CRM_PROSPECTS=901219609612
